-- =====================================================================
-- FinvQuant PostgreSQL V100004：区域字典初始数据（finv_region）
--
-- 决策（ACANX 2026-08-05）：
--   - 数据来源：富途自选股 region 字段统计（0~24）；
--   - idx 编码：保留 0（未知/默认）与 1（全球/外汇/加密货币），
--     其余区域 idx = 原值 + 99（101~123），为 2~100 段预留扩展空间；
--   - region 简写：ISO 3166 风格，与 finv_exchange.region 取值习惯一致
--     （CN / HK / USA / JP / SG / TW / IN 等）；
--   - 依赖 V11 放宽 idx CHECK 约束（允许 0）先执行；
--   - 幂等插入（ON CONFLICT (idx) DO NOTHING）。
-- 迁移策略：与既有迁移一致，整个 V100004 在一个事务内执行；失败自动回滚。
-- =====================================================================

BEGIN;

INSERT INTO finv_region (idx, region) VALUES
    (0,   'UNK'),     -- 未知/默认
    (1,   'GLOBAL'),  -- 全球/外汇/加密货币
    (101, 'CN'),      -- 中国大陆
    (102, 'HK'),      -- 香港
    (103, 'USA'),     -- 美国
    (104, 'JP'),      -- 日本
    (105, 'SG'),      -- 新加坡
    (106, 'MY'),      -- 马来西亚
    (107, 'AU'),      -- 澳大利亚
    (108, 'CA'),      -- 加拿大
    (109, 'DE'),      -- 德国
    (110, 'GB'),      -- 英国
    (111, 'IT'),      -- 意大利
    (112, 'TW'),      -- 台湾
    (113, 'FR'),      -- 法国
    (114, 'NL'),      -- 荷兰
    (115, 'PT'),      -- 葡萄牙
    (116, 'BE'),      -- 比利时
    (117, 'KR'),      -- 韩国
    (118, 'IN'),      -- 印度
    (119, 'ES'),      -- 西班牙
    (120, 'GR'),      -- 希腊
    (121, 'ZA'),      -- 南非
    (122, 'BR'),      -- 巴西
    (123, 'ID')       -- 印度尼西亚
ON CONFLICT (idx) DO NOTHING;

COMMIT;
