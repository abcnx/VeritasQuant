PolicyVersion: V1
ApprovalStatus: APPROVED
ApprovedBy: ProjectAuthor（当前会话确认）
ApprovedAt: "2026-07-31T04:15:00Z"
AllowedLicenses:
  - MIT
  - Apache-2.0
  - BSD-3-Clause
  - PSF-2.0
  - LGPL-3.0-only
  - MPL-2.0
PackageExceptions:
  # P2-031 Streamlit GUI 依赖（2026-08-03 审阅）：元数据 License 字段为
  # 非标准字符串变体，实质均为已批准宽松许可证（MIT/BSD/Apache 家族）。
  altair: BSD-3-Clause
  numpy: BSD-3-Clause
  packaging: Apache-2.0
  pandas: BSD-3-Clause
  pillow: MIT
  protobuf: BSD-3-Clause
  python-dateutil: BSD-3-Clause
  tenacity: Apache-2.0
VulnerabilitySla:
  CriticalHours: 24
  HighHours: 72
  MediumDays: 30
  LowDays: 90
# 2026-08-02 依赖审阅：新增 psycopg[binary] 运行时依赖（P2-002 数据库访问），
# psycopg 3.x 与 psycopg-binary 采用 LGPL-3.0-only 许可证，批准加入白名单。
# 2026-08-03 依赖审阅：新增 httpx（dev 依赖，FastAPI TestClient）传递引入
# certifi（TLS 证书包）采用 MPL-2.0 许可证（file-level copyleft，宽松可商用），
# 批准加入白名单。
