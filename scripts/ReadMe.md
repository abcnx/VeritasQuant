# 源码树维护脚本

本目录只保存源码树维护、依赖锁校验、前置检查、打包验证、证据收集和短期环境诊断脚本，不提供正式业务命令，也不进入 wheel。正式人工、CI 和运维业务命令必须通过 `src/veritasquant/cli/` 中由 `pyproject.toml` 声明的 console script 暴露。

`temporary/` 如被创建，其中每个脚本必须声明负责人、用途、创建日期和最迟清理日期，并且不得被生产代码、任务或测试导入。
